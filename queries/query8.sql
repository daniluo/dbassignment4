-- What is the total length of each playlist in hours? List the playlist id and name of only those playlists that are longer than 2 hours, along with the length in hours rounded to two decimals.
-- first takes the length of each track in each playlist, sums that for each playlist, converts to hours and rounds to 2 decimals, then filters for playlists longer than 2 hours
SELECT playlistid, name, ROUND(SUM(Milliseconds)/3600000.0, 2) as LengthInHours FROM playlists JOIN playlist_track ON playlists.playlistid = playlist_track.playlistid JOIN tracks ON playlist_track.trackid = tracks.trackid GROUP BY playlistid, name HAVING LengthInHours > 2;